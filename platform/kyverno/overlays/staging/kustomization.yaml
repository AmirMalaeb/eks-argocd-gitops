apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Staging environment: all policies at their target mode (no overrides).
# Enforce: require-approved-registries, require-resource-limits, disallow-privileged
# Audit: require-labels, require-probes
