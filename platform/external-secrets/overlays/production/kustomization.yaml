apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Production environment: patch ExternalSecrets to reference production/* paths in AWS Secrets Manager.
patches:
  - target:
      kind: ExternalSecret
      name: retail-store-db-credentials
    patch: |
      - op: replace
        path: /metadata/namespace
        value: retail-store-production
      - op: replace
        path: /spec/data/0/remoteRef/key
        value: production/retail-store/mariadb
      - op: replace
        path: /spec/data/1/remoteRef/key
        value: production/retail-store/redis

  - target:
      kind: ExternalSecret
      name: retail-store-slack-webhook
    patch: |
      - op: replace
        path: /metadata/namespace
        value: retail-store-production
      - op: replace
        path: /spec/data/0/remoteRef/key
        value: production/retail-store/slack
